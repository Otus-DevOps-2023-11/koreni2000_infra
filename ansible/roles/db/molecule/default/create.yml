---
galaxy_info:
  role_name: linux_administration
  namespace: glennbell
driver:
  name: vagrant
  provider:
    name: parallels
lint: "yamllint"
platforms:
- name: instance
  box: bento/ubuntu-20.04-arm64
provisioner:
  name: ansible
  lint:
    name: ansible-lint

# dependency:
#   name: galaxy
# driver:
#   name: vagrant
#   provider:
#     name: virtualbox
# platforms:
#   - name: instance
#     box: ubuntu/xenial64
# provisioner:
#   name: ansible
#   lint:
#     name: ansible-lint
# verifier:
#   name: ansible
